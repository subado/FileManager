#ifndef FILE_MANAGER_HPP
#define FILE_MANAGER_HPP

#include <filesystem>

namespace fs = std::filesystem;

class FileManager
{
public:

	FileManager();

	~FileManager() = default;
};

#endif // FILE_MANAGER_HPP
